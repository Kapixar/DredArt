
- new UI look, more concise, new errors, message popups

- 3 modes after insert: Holo, map, legacy holo

- 1 color holo on whole ship 
- paint codes holo on whole ship, in 1,2 or 3 batches that can be swapped between
- check errors bp - all into one shade of blue

- holo modes:
    - legacy
    - bp where you have one (or more) bp thaty you place once, and chnage textures 
    (bad placement visiblisity)
    - one color on whole ship that you change for each color
    - merge mode (giga good?)

- legacy holo upgrade, show little studs on 

- crowd detection, smart thing that speeds up painting with telling where to paint to minimalize movemnt

- last used pictures saved in chrome.local
 
- DOWNS of new holo: 
 - 1 inventory spot less 
 - cant be used in lockdown

- auto bp swapper after click (find scanner in eq and paste new str, if no just copy for the user)



let buildCommands = []
for(let y=0; y<canvas.height; y++){
    let spots = []
    for(let x=0; x < canvas.width; x++){
        let i = pxID(x, y)
        spots.push(data[i] == colorID ? 0 : 1)
    }
    fIx = spots.indexOf(1)
    if(fIx == -1) continue;
    lIx = spots.lastIndexOf(1)
    if(lIx - fIx > 64){
        cIx = Math.floor((lIx + fIx) / 2)
        buildCommands.push(144, 0, generateTag(fIx), generateTag(y), 161, parseInt(spots.slice(fIx, cIx).reverse().join(''), 2), 145)
        buildCommands.push(144, 0, generateTag(cIx), generateTag(y), 161, parseInt(spots.slice(cIx, lIx).reverse().join(''), 2), 145)

    } else {
        buildCommands.push(144, 0, generateTag(fIx), generateTag(y), 161, parseInt(spots.slice(fIx, lIx).reverse().join(''), 2), 145)
    }
}

- mosaic transform to settings and guide
- add refresh button next to corners
- refactor top bar to three bars: move, menu, close